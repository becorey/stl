#Boilerplate for a new MeteorJS project.

##Usage (in terminal):

```
meteor create yourapp
cd yourapp
git clone https://github.com/becorey/meteor-boilerplate.git
rm -rf .git #remove meteor-boilerplate from your git history
git init
```